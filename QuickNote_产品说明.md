# QuickNote · 产品说明与设计定位
> 给 Codex 的前置说明，请在开始编程前完整阅读

---

## 一、这是一个什么产品

QuickNote（快签）是一款 Windows 桌面笔记工具。

它的核心价值只有一句话：**在不打断当前工作的情况下，随时捕获任何内容，并能把某篇笔记钉在屏幕上持续编辑。**

它不是 Notion 那种重型知识库，也不是系统自带的简陋便签。它定位在两者之间——比便签更结构化，比 Notion 轻十倍。

---

## 二、核心使用场景

**场景 A：即时捕获**
用户正在浏览网页学习，遇到新知识点，按下全局快捷键，一个便签弹窗出现在屏幕上。用户输入或粘贴内容，选择归档收藏夹，按 Ctrl+Enter 保存，弹窗消失，继续浏览。全程不超过 10 秒，不切换 App，不打断思路。

**场景 B：持续编辑**
用户正在构思某个项目，从主界面打开某篇笔记，将它设为置顶浮动窗口，拖到屏幕一角。之后边看网页边往这篇笔记里追加文字、粘贴图片、录下想法，窗口始终悬浮可见。

这两个场景决定了所有的设计决策，编程时请始终以这两个场景为出发点判断优先级。

---

## 三、产品名称

- 英文名：**QuickNote**
- 中文名：**快签**
- App 内统一使用英文 **QUICKnote** 作为展示名称

---

## 四、整体设计风格

**风格定位：克制、清晰、不花哨。**

参考方向是 Linear 和早期 Notion——字体层级清晰，留白充足，每个元素都有存在的理由，没有多余的装饰。

具体规范：
- 主色调：白色 + 浅灰背景（`#F5F5F0` 左右）
- 唯一强调色：黄绿色（`#A8C940` 左右），仅用于选中态、保存按钮、归档选择器文字，不大面积铺色
- 所有弹窗：纯白背景，圆角 16px，明显投影阴影
- 卡片：白色圆角，轻微阴影，间距 16px，有呼吸感
- 字体：系统默认中文字体，标题加粗，正文正常字重，标签和时间戳小字弱化

**视觉的最高优先级是层次清晰**——用户要能一眼分辨哪个窗口在最前面、哪些是背景内容。

---

## 五、App 结构总览

```
QuickNote
├── 系统托盘（常驻后台）
│   ├── 快速记录（唤起便签）
│   ├── 打开主界面
│   └── 退出
│
├── 快捷便签（全局快捷键 Ctrl+Shift+N 唤起）
│   ├── 置顶浮动窗口，可拖拽
│   ├── 归档选择器（收藏夹 / 笔记）
│   ├── 文字输入 + 图片粘贴
│   ├── 录音、高亮、加粗工具
│   └── Ctrl+Enter 保存 / Esc 关闭
│
├── 主界面（笔记工作台）
│   ├── 左侧：收藏夹列表 + 设置入口
│   ├── 顶部：搜索框 + 排序 + 最小化
│   └── 右侧：笔记瀑布流（三列，卡片网格）
│
└── 笔记详情弹窗（点击卡片弹出）
    ├── 独立浮动窗口，可拖拽、可置顶
    ├── 默认只读，双击进入编辑模式
    ├── 附件预览区（图片缩略图 + 录音波形）
    ├── 工具栏（编辑模式下显示）
    └── 关闭时自动保存
```

---

## 六、关键交互原则

1. **快捷便签是核心**，优先级最高，体验必须极度流畅，保存动作不超过一步
2. **弹窗关闭 ≠ 退出**，所有关闭按钮只是隐藏窗口，App 始终驻留托盘
3. **自动保存**，笔记详情弹窗关闭时自动保存，用户不需要手动点保存
4. **记忆用户习惯**，便签默认沿用上次收藏夹，排序方式记住上次选择
5. **新增/修改的笔记自动置顶**，在瀑布流中排到最前面

---

## 七、技术栈与平台

- 平台：Windows 桌面应用
- 技术栈：Electron + HTML / CSS / JS
- 数据存储：本地 JSON 文件，零云服务，零 AI API
- 存储路径：`用户文档/QuickNote/`，包含 `data.json`、`config.json`、`assets/` 文件夹

---

## 八、Electron 窗口实现要求（重要）

所有弹窗（便签 + 笔记详情）必须实现真正的圆角无边框效果，**不能出现系统默认的方角边框**。

必须配置：
- `frame: false` — 去掉系统标题栏和边框
- `transparent: true` — 窗口背景透明
- `backgroundColor: '#00000000'` — 完全透明底色
- CSS 的 `body` 和 `html` 背景必须设为 `transparent`
- 圆角、阴影、背景色全部用 CSS 实现
- 拖拽区域用 `-webkit-app-region: drag` 实现，按钮区域用 `-webkit-app-region: no-drag` 覆盖

---

## 九、开发优先级

请严格按照以下顺序开发，不要一次性铺开所有功能：

| 阶段 | 内容 |
|------|------|
| P0 | Electron 初始化：透明窗口、托盘、全局快捷键框架 |
| P0 | 快捷便签：完整交互流程跑通（输入、归档、保存、图片粘贴） |
| P0 | 本地数据读写：JSON 存储、收藏夹与笔记的增删改查 |
| P1 | 主界面：瀑布流卡片布局、收藏夹切换、搜索 |
| P1 | 笔记详情弹窗：展示、编辑、置顶、自动保存 |
| P2 | 附件系统：图片、录音 |
| P2 | 排序：时间 / 名称 / 自定义拖拽 |
| P3 | 标签、设置页、私密收藏夹等扩展功能 |

**详细的界面结构、交互逻辑和技术实现细节请参考配套文档：《QuickNote 交互说明文档 v2》**
